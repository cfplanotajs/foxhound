# Prisma SQLite database file
DATABASE_URL="file:./prisma/dev.db"

# OpenAI API key used only for server-side provider calls
# Leave blank to use Demo Mode (Mock) only.
OPENAI_API_KEY=""

# Optional OpenAI image model override (defaults to app fallback when empty)
OPENAI_IMAGE_MODEL=""
