import { getQualityOptionsForModel } from "@/lib/providers/model-quality";
import { ProjectFolderSelector } from "./ProjectFolderSelector";
import { buttonSecondary, card, chipNeutral, fieldLabel, helperText, inputBase, sectionHeader } from "./ui";

type Preset = { id: string; name: string; version: string; description: string; defaultModel: string; defaultParams?: Record<string, unknown>; bestUseLabel?: string | null };
type ProjectFolder = { id: string; name: string; isArchived: boolean };
type Project = { id: string; name: string; isArchived: boolean; folders: ProjectFolder[] };

export function GenerationControls({ presetId, setPresetId, presets, selectedPreset, provider, setProvider, model, setModel, aspectRatio, setAspectRatio, setAspectRatioTouched, variationCount, setVariationCount, quality, setQuality, projectId, setProjectId, folderId, setFolderId, projects }: { presetId: string; setPresetId: (v: string) => void; presets: Preset[]; selectedPreset?: Preset; provider: string; setProvider: (v: string) => void; model: string; setModel: (v: string) => void; aspectRatio: string; setAspectRatio: (v: string) => void; setAspectRatioTouched: (v: boolean) => void; variationCount: number; setVariationCount: (v: number) => void; quality: string; setQuality: (v: string) => void; projectId: string; setProjectId: (v: string) => void; folderId: string; setFolderId: (v: string) => void; projects: Project[] }) {
  return <div className={card}><h2 className={sectionHeader}>1) Preset & Settings</h2><p className={`mb-2 ${helperText}`}>Choose provider, model, and generation controls.</p><label className="grid gap-1"><span className={fieldLabel}>Preset</span><select value={presetId} onChange={(e) => setPresetId(e.target.value)} className={inputBase}><option value="">Select preset</option>{presets.map((preset) => <option key={preset.id} value={preset.id}>{preset.name} ({preset.version})</option>)}</select></label>{selectedPreset ? <div className="mt-3 rounded-xl border border-slate-200 bg-slate-50 p-3 text-sm text-slate-700"><p><strong>{selectedPreset.name}</strong> <span className="text-slate-500">v{selectedPreset.version}</span></p>{selectedPreset.bestUseLabel ? <p className={`mt-1 inline-block ${chipNeutral}`}>{selectedPreset.bestUseLabel}</p> : null}{selectedPreset.description ? <p className="mt-1 text-slate-600">{selectedPreset.description}</p> : null}<p className="mt-1">Model: {selectedPreset.defaultModel} · Size: {String(selectedPreset.defaultParams?.size ?? "1024x1024")} · Quality: {String(selectedPreset.defaultParams?.quality ?? "high")}</p></div> : null}<div className="mt-3 grid grid-cols-1 gap-3 md:grid-cols-2"><div className="grid gap-1"><span className={fieldLabel}>Provider</span><div className="inline-flex rounded-xl border border-slate-300 bg-slate-100 p-1"><button type="button" onClick={() => setProvider("mock")} className={provider==="mock"?buttonSecondary:"rounded-lg px-3 py-1.5 text-xs font-semibold text-slate-600"}>Demo Mode</button><button type="button" onClick={() => setProvider("openai")} className={provider==="openai"?buttonSecondary:"rounded-lg px-3 py-1.5 text-xs font-semibold text-slate-600"}>OpenAI</button></div></div><label className="grid gap-1"><span className={fieldLabel}>Model</span>{provider === "mock" ? <select value={model} onChange={(e) => setModel(e.target.value)} className={inputBase}><option value="mock-v1">mock-v1</option></select> : <select value={model} onChange={(e) => setModel(e.target.value)} className={inputBase}><option value="gpt-image-2">gpt-image-2</option><option value="gpt-image-1">gpt-image-1</option><option value="dall-e-3">dall-e-3</option><option value="dall-e-2">dall-e-2</option></select>}</label><label className="grid gap-1"><span className={fieldLabel}>Aspect Ratio</span><select value={aspectRatio} onChange={(e) => { setAspectRatio(e.target.value); setAspectRatioTouched(true); }} className={inputBase}><option value="1:1">Square 1:1</option><option value="2:3">Portrait 2:3</option><option value="4:6">Portrait 4:6</option><option value="4:3">Landscape 4:3</option><option value="3:2">Classic 3:2</option><option value="9:16">Vertical 9:16</option><option value="16:9">Widescreen 16:9</option></select></label><label className="grid gap-1"><span className={fieldLabel}>Variations</span><select value={variationCount} onChange={(e) => setVariationCount(Number(e.target.value))} className={inputBase}><option value={1}>1</option><option value={2}>2</option><option value={4}>4</option></select></label><label className="grid gap-1"><span className={fieldLabel}>Quality</span><select value={quality} onChange={(e) => setQuality(e.target.value)} className={inputBase}>{getQualityOptionsForModel(provider as "openai" | "mock", model).map((q) => <option key={q} value={q}>{q}</option>)}</select></label><ProjectFolderSelector projects={projects} projectId={projectId} folderId={folderId} onProjectChange={(nextProjectId) => { setProjectId(nextProjectId); setFolderId(""); }} onFolderChange={setFolderId} /></div></div>;
}
