type Preset = { id: string; name: string; samplePrompt?: string | null };

export function PromptComposer({ presets, setPresetId, setSinglePrompt, error, singlePrompt, setSinglePromptValue, bulkPrompts, setBulkPrompts, constraints, setConstraints, loading, formValid, submitJob }: { presets: Preset[]; setPresetId: (id: string) => void; setSinglePrompt: (value: string) => void; error: string; singlePrompt: string; setSinglePromptValue: (value: string) => void; bulkPrompts: string; setBulkPrompts: (value: string) => void; constraints: string; setConstraints: (value: string) => void; loading: boolean; formValid: boolean; submitJob: () => void }) {
  return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h2 className="mb-1 text-lg font-semibold">Prompt</h2><p className="mb-3 text-sm text-slate-600">Describe the image or paste multiple prompts for batch generation.</p><div className="mb-3 flex flex-wrap gap-2">{presets.map((p) => <button key={p.id} type="button" className="rounded-full border border-slate-300 bg-white px-3 py-1.5 text-xs font-semibold hover:bg-slate-100" onClick={() => { setPresetId(p.id); if (p.samplePrompt) setSinglePrompt(p.samplePrompt); }}>{p.samplePrompt ? `${p.name} sample` : `Use ${p.name}`}</button>)}</div>{error ? <p className="mb-2 rounded bg-rose-100 p-2 text-sm text-rose-700">{error}</p> : null}<label className="grid gap-1 text-sm"><span className="font-medium">Single Prompt</span><textarea value={singlePrompt} onChange={(e) => setSinglePromptValue(e.target.value)} className="min-h-28 rounded-xl border p-3" placeholder="Describe subject, composition, style, and lighting..." /></label><label className="mt-2 grid gap-1 text-sm"><span className="font-medium">Bulk Prompts (one per line)</span><textarea value={bulkPrompts} onChange={(e) => setBulkPrompts(e.target.value)} className="min-h-24 rounded border p-2" /></label><label className="mt-2 grid gap-1 text-sm"><span className="font-medium text-slate-600">Production Constraints (optional)</span><textarea value={constraints} onChange={(e) => setConstraints(e.target.value)} className="min-h-16 rounded border p-2" /></label><p className="mt-2 text-xs text-slate-500">Submit is enabled when preset, provider, model, and at least one prompt line are present.</p><button disabled={loading || !formValid} className="mt-3 rounded-xl bg-blue-600 px-4 py-3 font-semibold text-white disabled:cursor-not-allowed disabled:opacity-50" onClick={submitJob}>{loading ? "Generating..." : "Generate Images"}</button></div>;
}
