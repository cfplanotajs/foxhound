import { softCard } from "./ui";

type Step = { label: string; state: "pending" | "active" | "complete" };

export function HeroHeader({ provider, showManager, onToggleManager, steps, nextAction }: { provider: string; showManager: boolean; onToggleManager: () => void; steps: Step[]; nextAction: string }) {
  return <header className={softCard}><h1 className="text-3xl font-bold text-slate-900">Foxhound Studio Console</h1><p className="mt-2 text-slate-600">Start in Demo Mode, generate images, edit your favorite, then export an approved ZIP.</p><div className="mt-3 flex flex-wrap gap-2 text-xs font-semibold"><span className={`rounded-full px-3 py-1 ${provider === "mock" ? "bg-indigo-100 text-indigo-700" : "bg-slate-100 text-slate-500"}`}>Start in Demo Mode</span><span className={`rounded-full px-3 py-1 ${provider === "openai" ? "bg-violet-100 text-violet-700" : "bg-slate-100 text-slate-500"}`}>OpenAI Mode</span><span className="rounded-full bg-emerald-100 px-3 py-1 text-emerald-700">Background worker</span></div><div className="mt-4"><p className="mb-2 text-xs font-semibold uppercase tracking-wide text-slate-500">Guided demo flow</p><div className="grid grid-cols-2 gap-2 text-sm md:grid-cols-5">{steps.map((step) => <div key={step.label} className={`rounded-xl px-3 py-2 ${step.state === "complete" ? "bg-emerald-100 text-emerald-800" : step.state === "active" ? "bg-blue-50 text-blue-800" : "bg-slate-100 text-slate-500"}`}>{step.label}</div>)}</div><p className="mt-2 text-sm text-slate-700">Next up: {nextAction}.</p></div><div className="mt-4"><button className="rounded-xl bg-slate-800 px-4 py-2 text-white" onClick={onToggleManager}>{showManager ? "Hide Preset Manager" : "Manage Presets"}</button></div></header>;
}
