type ProjectFolder = { id: string; name: string; isArchived: boolean };
type Project = { id: string; name: string; isArchived: boolean; folders: ProjectFolder[] };

export function ProjectFolderSelector({ projects, projectId, folderId, onProjectChange, onFolderChange }: { projects: Project[]; projectId: string; folderId: string; onProjectChange: (projectId: string) => void; onFolderChange: (folderId: string) => void }) {
  return <div className="rounded-xl border border-slate-200 bg-slate-50 p-3 md:col-span-2"><p className="mb-2 text-xs text-slate-500">Projects keep jobs organized for studio workstreams.</p><div className="grid grid-cols-1 gap-3 md:grid-cols-2"><label className="grid gap-1 text-sm"><span className="font-medium">Project</span><select value={projectId} onChange={(e) => onProjectChange(e.target.value)} className="rounded border p-2"><option value="">Unassigned</option>{projects.map((p) => <option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label className="grid gap-1 text-sm"><span className="font-medium">Folder</span><select value={folderId} onChange={(e) => onFolderChange(e.target.value)} className="rounded border p-2" disabled={!projectId}><option value="">Unassigned</option>{(projects.find((p) => p.id === projectId)?.folders ?? []).map((f) => <option key={f.id} value={f.id}>{f.name}</option>)}</select></label></div></div>;
}
